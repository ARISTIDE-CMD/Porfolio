import DesignPage from "@/components/Design";

export const metadata = {
  title: 'Design d’interface | ArisDev',
  description: 'Interfaces claires et fonctionnelles, orientées expérience utilisateur.',
};

export default function DesignServicePage() {
  return <DesignPage />;
}